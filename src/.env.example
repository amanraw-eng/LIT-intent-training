# Copy to src/.env. Do not commit the resulting .env file.
# Only secrets / deployment-specific overrides belong here - every other
# setting (model type, device, audio limits, host/port, log level) lives in
# app/core/config.py.

HF_TOKEN=
HF_MODEL_REPO=amn-raw/whisper-small-intent17-classifier
